before_script: []
stages: [
  "build",
]

documentation:
  image: "timnn/texlive"
  stage: "build"
  script: [
      "cd documentation",
      "pdflatex -synctex=1 --shell-escape -interaction=nonstopmode Main.tex"
      ]
  artifacts:
    paths:
      - documentation/Main.pdf
